# Environment variables with passwords and tokens — NEVER committed
config/.env

# Instance-specific Claude context (domain, names, hardware)
CLAUDE.local.md

# CouchDB data
/data/

# macOS
.DS_Store

# Editor
.vscode/
*.swp
